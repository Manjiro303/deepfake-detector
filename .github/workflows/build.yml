name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    name: Install & Test
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 📦 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: 📥 Install
        run: npm install
      
      - name: ✅ Success
        run: echo "✨ Dependencies installed successfully!"

  build:
    name: Build APK (Optional)
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'workflow_dispatch'
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 📦 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: 📥 Install
        run: npm install
      
      - name: 🔐 Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      
      - name: 🎯 Initialize EAS Project
        run: eas init --id $(uuidgen) --non-interactive || true
      
      - name: 📱 Build APK
        run: eas build --platform android --profile preview --non-interactive
